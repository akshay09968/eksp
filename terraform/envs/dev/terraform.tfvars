# dev overrides. Sizing lives in main.tf — these are the user-specific knobs.

# region = "us-east-1"

# Lock the API endpoint to your IP (strongly recommended):
# endpoint_public_access_cidrs = ["203.0.113.7/32"]

# After the first push of this repo:
# gitops_repo_url = "https://github.com/<you>/<repo>.git"
