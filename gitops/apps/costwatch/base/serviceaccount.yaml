# Name must match the Pod Identity association in terraform/envs/*/main.tf —
# this SA is what maps the pod to the Cost Explorer read-only role.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: costwatch
