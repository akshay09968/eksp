apiVersion: v1
kind: Namespace
metadata:
  name: istio-ingress
  labels:
    # Gateway pods are mesh-native Envoys (not ambient-enrolled); baseline PSS —
    # they need no host access but do bind low-numbered listener ports in-pod.
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: baseline
